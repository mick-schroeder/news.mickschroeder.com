- build http://localhost:8000/sources page to show all sources
    - data page use shadcn table

- tags filter acts a bit weird for entries with no tags. when 1 option is unselected all of the sudden i guess since all isn't selected anymore it filters out all that cat + all the blanks

- add website screenshot to detail page

- mechnism for sites falling off list?